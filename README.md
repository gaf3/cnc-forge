# cnc-forge

Code and Changes Forge

# yaml

```yaml
description:
input:
  fields:
  generate:
  validate:
output:
  code:
  - github:
      repo:
      branch:
      pull_request:
    change:
    - github:
        repo:
        branch:
      content:
      - source:
        destination:
        exclude:
        include:
        preserve:
        transform:
        condition:
        iterate:
        generate:
      condition:
      iterate:
      generate:
    condition:
    iterate:
    generate:
  condition:
  iterate:
  generate:
```

# cnc-forge

Code and Changes Forge

# yaml

```yaml
description:
input:
  fields:
  generate:
  validate:
output:
  code:
  - github:
      repo:
      branch:
      pull_request:
    change:
    - github:
        repo:
        branch:
        pull_request:
      content:
      - source:
        destination:
        preserve:
        condition:
        iterate:
        generate:
      condition:
      iterate:
      generate:
    condition:
    iterate:
    generate:
  condition:
  iterate:
  generate:
```

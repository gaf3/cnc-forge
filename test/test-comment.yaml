description: Test forge
output:
  code:
  - github:
      repo: "{{ craft }}"
      prefix: "{{ ticket }}"
      comment: /approve
    change:
    - github:
        repo: gaf3/test-forge
      content:
      - source: /
